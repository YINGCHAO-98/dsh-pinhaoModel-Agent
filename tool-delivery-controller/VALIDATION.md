# 本地验证记录

## 跨会话过期预留修复（2026-09-22）

会话导出 `session-21d513cf-10f1-4a85-8c5d-2c16c4a8a93f` 复现：空项目和新会话的 `delivery_status` 为空，但同一绝对工作区路径在全局 `~/.dsh-delivery` 中存在其他会话遗留的过期 blocked project，因此 `delivery_start` 被拒绝且当前会话拿不到可恢复/取消的 ID。对应记录停在 implement，快照等于基础快照，验证、修复和同步均未开始，冷却已过期。

修复后，只有满足上述全部无副作用条件的过期预执行占位会自动转为 failed 并记录 `reservation.expired`；仍在冷却、已经进入验证或具有冲突等状态的任务继续阻塞，并在错误中返回具体 delivery id 和跨会话归属提示。98 项单元/状态机测试和 7 项真实 DSH 集成测试通过；新增正反路径均有覆盖。未修改用户真实运行数据库，加载新版 preset 后下一次启动会按规则回收该条过期占位。

日期：2026-09-20。环境：macOS、DSH Desktop 自带 Node.js v24.9.0；真实 DSH 测试使用本机 deepseek-harness 构建产物。原生工具实现从当前 Desktop 安装包加载。

| 检查 | 结果 |
| --- | --- |
| 单元测试 `tests/*.test.mjs` | 30/30：控制器强制验收、修复预算、恢复、快照绑定、路径保护、探索分页与撤销、实际工作副本差异、Worker 身份绑定、独立质量失败/阻塞/恢复、质量证据绑定、五模型路由、DAG 并发/依赖/失败隔离、取消清理与图片快照传递及视觉角色工具白名单 |
| `tests/dsh.integration.mjs` | 2/2：真实工具注册器拒绝主 Agent 调用执行工具和旁路工具；Worker allowlist、摘要 schema 与释放 |
| `tests/sandbox.integration.mjs` | 3/3：真实验证环境的文件隔离、超时、零测试报告拒绝 |
| `tests/worker-sandbox.integration.mjs` | 3/3：原生 read/edit/write/glob/grep/bash 运行，工作区外私有文件读写拒绝、网络拒绝、环境变量不继承、工作目录边界、超时、取消、访问撤销，以及质量副本拒绝文件/Shell 写入 |
| `tests/dsh-e2e.integration.mjs` | 2/2：真实 YAML 加载 → 命令/工具双入口 → spawn Worker 按需探索与编辑 → Shell 自测 → 摘要 → 磁盘差异 → 独立验收失败 → 修复 → passed；随后进入独立 Kimi 门禁；确认五个专业入口、六个模型路由解析、Skill 实际加载、原项目不变和导出产物正确 |

共 40 项自动测试通过。端到端测试仅将 LLM 响应替换为确定性 fixture，未调用付费模型。未重载正在运行的 DSH 会话；新配置由重新加载 preset 的会话使用。

外层沙箱禁止嵌套 Seatbelt，因此真实沙箱和端到端测试通过获准的沙箱外测试进程执行。所有测试项目和隔离副本位于临时目录，结束后清理。

Worker 原生工具链当前仅支持 macOS Seatbelt；Docker Worker 运行时未接入并明确阻止启动。验证 Runner 的 Docker 分支保留但未实测。除下述真实加法修复任务外，未验证其他任务的模型输出质量、远端部署、恶意脱离进程组的后台进程完全回收。默认合同仍仅允许修改 src/，保护 tests/ 和包配置；依赖不自动安装。详见 README.md。

上述 40 项自动测试的模型响应均为 fixture，不验证付费模型服务、鉴权或多模态远端兼容性。真实模型验证结果另见下节。图片桥接以附件服务 stub 验证数据边界；本次未进行真实图片模型分析。当前没有原生音视频传输、联网研究、ArkCLI 生图/生视频执行通道。专业节点同步执行，不恢复旧版后台 jobs 和动态 workflow 脚本。

## 真实模型端到端：已通过

2026-09-20 16:39:16–16:41:05（Asia/Shanghai），耗时 108.43 秒。运行安装包中的生产 DSH AgentLoop、pi-ai 适配器、凭据服务、spawn 子 Agent 和当前 preset；没有替换模型响应，也没有由测试程序直接调用 delivery_start。使用现有 doubao 方舟 Plan 路由，凭据仅经 DSH 凭据服务读取。

任务：修复临时项目中错误的 CommonJS 加法函数，覆盖正数、负数和零，不修改已有测试或配置。

| 角色 | 实际模型 | 真实请求数 | 结果 |
| --- | --- | ---: | --- |
| 根调度 | deepseek-v4-1-flash | 4 | 接收自然语言需求，加载 Skill，自行调用 delivery_start，查询最终状态并回复 |
| 实现子 Agent | deepseek-v4-1-flash | 7 | 读取代码与测试，把 a - b 修复为 a + b，执行自测并提交结构化结果 |
| 独立质量子 Agent | kimi-k2.7-code | 7 | 独立读取代码、执行已有测试及五类边界冒烟，返回 passed |

控制器最终状态 `passed`；实现 1 轮，验证 1 轮，修复 0 轮。合同测试为 2 pass / 0 fail。测试运行器额外独立调用导出代码检查 4 组输入全部正确，并逐字节确认已有测试未改变；原项目仍保留错误实现，正确代码只在交付产物中。24 次工具调用没有工具协议错误（例如 git status 在无 Git 仓库中退出非零，作为实际命令结果记录）。

- 交付 ID：`b78bcb26-0923-4f74-9a97-cc13303ac652`。
- 代码快照：`a14a3f3cf1621038aaa6b91ed6413f7062d7d80d342fbfd61b9d9df6d8415df6`，Kimi 报告与合同检查绑定同一快照。
- [持久化验证摘要](validation/live-20260920.json)：包含 18 个真实服务响应 ID、模型/会话身份、Token 用量字段、产物路径及测试未变证明。
- 完整本地报告：`/var/folders/ft/wyt29s3x3_s3c9f9bdqv2rhw0000gn/T/pinhaomo-live-Uu9wie/report.json`；同目录保留完整 trace、状态数据库和产物，系统可能清理临时目录。
- 重跑入口：`DSH_LIVE=1 npm run test:live`，会产生真实模型调用费用。

结论仅覆盖真实 DeepSeek→Kimi 研发交付链及此小型任务。Kimi 研究、GLM、MiniMax、豆包媒体与多专业 DAG 尚未完成真实模型任务验证；不能用本结果声称所有模型均已打通。运行器使用独立临时会话，没有重载正在运行的 Desktop 会话。

## Worker 工具可见性与身份修复（2026-09-20）

本次确认：此前 guard 拒绝主 Agent 的执行工具调用，但模型请求仍包含这些工具。现在 `tool-policy.mjs` 用同一策略过滤 `system-prompt/assemble` 返回的工具 schema 并检查实际执行权限。没有对祖先 scope 使用 deny mask，以免同时屏蔽子 Agent；原始注册器/管理 UI 仍可能列出已注册工具，实际模型请求已经过滤。

Worker 和专业子 Agent 统一通过 `bind()` 绑定身份，首次请求组装等待尚未完成的绑定。拒绝原因区分 ROOT_TOOL_NOT_ALLOWED、WORKER_NOT_BOUND、WORKER_EXPIRED 和 WORKER_TOOL_NOT_ALLOWED。取消、释放、跨子会话和角色越权仍拒绝。

本次 45 项不同测试通过：单元与真实注册器 39 项；真实 DSH 端到端 3 项；Worker Seatbelt 3 项。覆盖完整 preset 的主 Agent schema 过滤、合法 Worker 编辑/Shell/修复、专业角色、独立质量门禁、并发身份隔离和取消。端到端采用固定模型响应，未发送真实模型 API 请求。没有重载正在运行的用户会话，也没有在本次记录中复现用户所报告的具体历史调用；上述结论针对确认的代码缺陷和回归测试。

## 受控能力申请与交接验证（2026-09-20）

本次最终组合代码通过 49 项测试：38 项单元测试、2 项真实 DSH 工具注册测试、3 项真实 DSH/沙箱端到端测试、6 项验证/Worker 沙箱测试。外层沙箱不能嵌套 Seatbelt，真实沙箱测试经授权在外层之外运行，只使用临时项目。集成测试包括简单任务不额外委派、Worker 请求专业协助、修复轮自动恢复报告、报告缺失或被篡改阻断交付、未读图/未运行检查不能靠自报 passed 通过、别名与 DAG 共用预算、禁止专业节点再委派。

两个真实模型样例均通过，详见 [验证摘要](validation/capabilities-20260920.json)：

| 场景 | 耗时 | 模型响应数 | 专业申请 | 结果 |
| --- | ---: | --- | ---: | --- |
| 简单加法 bug | 230.367 秒 | DeepSeek 12、Kimi Code 17 | 0 | 合同和独立审查通过；原测试未变 |
| 修复 + 明确要求的发布文案 | 192.736 秒 | DeepSeek 13、MiniMax 2、Kimi Code 12 | 1 | 专业报告接受后通过 reportRefs 自动进入研发；原测试未变 |

两个样例均只改临时副本，不重载正在使用的 Desktop 会话。协助样例显式要求专业文案，用于验证调用和交接，不作为自主路由准确率证据。样本太少、任务不完全相同且调用有重叠，不以两者耗时比较模型优劣。运行期间同目录另有工具可见性/子会话绑定更新；真实样例反映启动时加载的代码，最终组合修改另由上述 49 项测试覆盖。

记录已保留服务端上报 Token 字段；缺失用量不估算为零，不根据 Token 总数推断货币费用。简单修复约四分钟、29 次响应，说明下一优先级应是建立真实 bug 案例基线，评估固定独立审查的收益和开销，尚不适合宣称已优化选模或成本。

Skill 的 quick_validate.py 因当前 Python 缺少 PyYAML 未运行成功；已使用安装包 YAML 解析器检查元数据，并由真实 DSH 集成测试验证 Skill 加载。


## 总 agent 汇总与验收后同步（2026-09-20）

当前改动通过 63 项不同检查：54 项单元/真实 DSH 注册测试、3 项真实 DSH 端到端、6 项验证与 Worker Seatbelt 测试。最终同步与必需输入检查修改后，另重跑受影响的 30 项控制器/同步测试及 3 项端到端；全部通过。模型响应是 fixture，没有新增真实模型调用，也未重载用户当前 Desktop 会话。

覆盖 partial 不回写、project 默认自动回写、已验收分步产物导入、组合失败后修复、保留并行改动、同文件非重叠合并、冲突阻断、过期决定拒绝、同步后实际项目复核、同步中断恢复、暂存文件恢复、符号链接拒绝、身份隔离、证据和权限约束。自动质量门禁仅复用相同版本、目标、模型且完整的已通过报告，篡改拒绝；固定检查仍重新执行。

[验证记录及文件指纹](validation/project-delivery-20260920.json)。这些结果不等于真实模型规划效果达标。外部工具同步适配器与跨独立总会话产物共享尚未接入；文件同步也不是多文件全局原子事务。旧媒体输入反例不在本次修复范围。

Skill quick_validate.py 仍因缺少 PyYAML 无法运行；安装包 YAML 解析器已成功解析更新后的元数据，真实 preset 加载测试通过。旧评分 JSON 已标注 superseded，历史失败证据保留，旧总分与成熟度判断不再适用。


## 文档维护位置调整（2026-09-20）

交付约定与评价制度已分别迁至 [WORKFLOW.md](WORKFLOW.md) 和 [EVALUATION.md](EVALUATION.md)，根 README 与控制器 README 提供长期入口。AGENTS.md 仅保留临时配置维护规则；运行时控制器与配置未因本次迁移改变。历史验证记录的路径和指纹仍保留当时值，本次只检查内容迁移完整性、链接和引用，不把历史测试说成本次重跑。


## 登记任务与宿主冲突决定硬约束（2026-09-20）

本轮 74 项不同检查通过：63 项单元/真实 DSH 注册检查、5 条真实 DSH 端到端路径、6 项验证/Worker Seatbelt 隔离检查。端到端包含普通命令、工具启动、专业协助、宿主冲突回答、登记子任务后总任务集成。模型响应和宿主用户回答均为 fixture，未调用真实模型或向真人发出测试问题，未重载正在使用的 Desktop 会话。记录见 [实现指纹与检查范围](validation/enforced-coordination-20260920.json)。

负向检查覆盖：缺少任务上下文、依赖成环、修改范围扩大、伪造验收命令、任务清单删项/换归属、直接启动登记子任务、失败/篡改子产物、未结束总任务旁路；模型传入冲突选择、伪造状态转换、无交互提供方、缺失/错误问题 ID、取消/自定义回答、等待期间文件变化及回答重复使用。正向检查覆盖固定并发、依赖交接、必须完成总任务验收、首次汇入冲突持久化后处理、回答消费后重新验收与同步。

真实宿主 userQuestions.ask 校验活跃根调用者；控制器创建问题、接收答案、落盘绑定交付版本，模型工具不能代填。此保证以宿主交互服务为信任边界，不声称通过模型转述证明人类身份。自然语言任务拆分与验收语义仍需评估。

## 快照预算与专业输入范围修复（2026-09-20）

旧代码固定 4 MiB/1000 文件，单图专业请求也采集整个目录。pinhaoModel 目录只读测量约 198 MB，包含 npm 缓存、约 104 MB 帧文件和约 52 MB ffmpeg。修复后项目快照默认 256 MiB/10000 文件，排除依赖缓存；非质量专业任务按显式文件引用采集，质量审查保持完整快照。

实际目录只读 capture 成功：220 文件、169409991 字节；只引用 preview.png 时仅采集 361222 字节。未修改该项目文件或发送模型请求。

本次 78 项测试通过：单元与注册器 67 项，真实 DSH 端到端/沙箱 11 项。新增覆盖旧阈值以上快照、精确边界、超限诊断、缓存排除、产物严格检查、引用范围、路径穿越和符号链接拒绝。模型响应为 fixture。已加载旧插件的进程需重新加载或重启。
