# 交付恢复修复（2026-09-21）

- 新增模型可调用 `delivery_cancel({id})`，复用用户命令的执行路径与取消清理逻辑。只允许根 Agent 操作本会话记录，子 Agent、跨会话、伪造参数被拒绝；不删除项目文件、不回滚同步。
- 已终结交付重复取消返回原终态，不因重复请求再次报错。
- 新建任务时持有工作区锁，仅自动结束本会话、同工作区、非必需子任务、旧版明确 `Worker did not complete: max-tokens` 的记录；不清除其他会话、环境故障或冲突。
- 保留上轮新增的输出耗尽失败分类、禁止原样恢复和原生 Todo 进度推送。

验证：76 项单元测试通过，3 项原 DSH 集成回归通过，新增 Worker 输出超限分类与子会话/工作副本清理集成测试通过，合计 80 项。集成模型为 fixture。`git diff --check` 通过。

实际运行处置：在 Desktop 停止旧根会话模型重试，通过现有 `/deliver cancel cb1ee3a3-4d93-4421-a353-b3e6fafa04b9` 收到 `cancelled`，数据库只读复核一致；随后重启 Harness 加载本次实现。没有更改 agent.cordis.yml，没有重跑动画生成。

在线验收限制：重启后要求原会话仅调用新增取消工具，但模型等待超过一分钟仍未发起工具调用，已停止该验收请求。新增工具注册和调用通过本地 DSH 集成测试，尚无在线模型成功调用的证据；本次不宣称已解决模型响应缓慢问题。
