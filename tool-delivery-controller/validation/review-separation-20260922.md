# 交付与审查分离验证

- 官方参考：https://learn.chatgpt.com/docs/code-review?surface=app 。Codex /review 针对已有改动给出可操作的审查发现，不修改工作区。
- 本次实现：preset 显式 on_request，默认交付不调用审查模型；delivery_review 只审查已完成交付的快照，独立记录审查状态。
- 程序约束：所属会话检查、完成状态检查、快照身份/路由/证据检查、只读审查路由、并发审查锁、超时/取消记录、策略不可变、本地检查和同步凭据门禁保留。
- 单元测试 129/129 通过；DSH 集成测试 9/9 通过。覆盖真实工具注册、临时沙箱文件同步、审查超时文件保留，模型输出使用模拟接口。
- 尚未以本次代码完成真实在线模型生成回放；不以集成测试冒充上游模型可用性证明。
- 旧 required 任务继续遵守原门禁；本次不改写已有交付记录，也不由 Codex 代写用户目标 HTML。
