---
name: deepseek-code-execution
description: 使用 DeepSeek 实现、修改、重构、调试和验证代码，并将变更落到目标文件。不用于独立的长文研究或纯视觉分析。
---

# DeepSeek 代码执行

先确认可验收行为、修改范围、适用约束和上游方案。搜索定位真实执行路径，只读取实现所需文件。

## 实现

- 保留用户已有修改，不改动无关文件。
- 进行最小而完整的修改，不用占位符代替可交付实现。
- 上游方案与实际代码冲突时，优先保护真实系统约束，并记录差异。
- 代码、样式和配置直接写入目标文件，不在返回消息中重复粘贴。

## 验证与交付

执行与变更风险相称的最小有效验证。返回状态、修改文件绝对路径、关键行为、验证命令与结果、限制和未解决问题。
